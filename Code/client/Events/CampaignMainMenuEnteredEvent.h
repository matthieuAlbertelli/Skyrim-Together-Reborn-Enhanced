#pragma once

// Semantic Skyrim lifecycle boundary. A fresh boot may emit this event too;
// campaign admission decides whether any runtime departure is required.
struct CampaignMainMenuEnteredEvent
{
};
